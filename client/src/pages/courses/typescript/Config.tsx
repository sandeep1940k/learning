import React from "react";

const Config: React.FC = () => {
    return (
        <>
        <h1>Config</h1>
        <div>
            <h2>tsconfig.json</h2>
            <p>The tsconfig.json file is a JSON format file where you may specify several options to inform the compiler how to compile a project. The presence of this file in the directory implies that it is the TypeScript project root.</p>
        </div>
        </>
    )
}

export default Config;